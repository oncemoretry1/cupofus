import { desc, eq } from "drizzle-orm";
import { getDb } from "../../../db";
import { books, savedCups } from "../../../db/schema";
import { getCurrentUser } from "../../../lib/auth";
export async function GET(request:Request){const user=await getCurrentUser(request);const owner=user?.email??new URL(request.url).searchParams.get("guestId");if(!owner)return Response.json({cups:[]});const cups=await getDb().select({id:savedCups.id,cupName:savedCups.cupName,answers:savedCups.answers,createdAt:savedCups.createdAt,bookId:savedCups.bookId,bookSlug:books.slug,bookTitle:books.title,coverColor:books.coverColor}).from(savedCups).leftJoin(books,eq(savedCups.bookId,books.id)).where(eq(savedCups.userEmail,owner)).orderBy(desc(savedCups.createdAt));return Response.json({cups,user})}
export async function POST(request:Request){const body=await request.json();const user=await getCurrentUser(request);const owner=user?.email??body.guestId;const {bookId,cupName,answers=[]}=body;if(!owner||!bookId||!cupName)return Response.json({error:"missing fields"},{status:400});const row=await getDb().insert(savedCups).values({userEmail:String(owner),bookId:Number(bookId),cupName:String(cupName).slice(0,100),answers:JSON.stringify(answers)}).returning();return Response.json({cup:row[0]})}
