"use client";
import { useEffect, useState } from "react";
import "./profile.css";
import "./onboarding.css";
export default function ProfilePage(){const [name,setName]=useState("");const [draft,setDraft]=useState("");useEffect(()=>{setName(window.localStorage.getItem("cup-of-us-name")??"")},[]);const save=()=>{const value=draft.trim();if(value){window.localStorage.setItem("cup-of-us-name",value);setName(value)}};return <main className="profile-page"><a className="logo" href="/"><span>CUP</span><i>of</i><span>US</span></a><section>{name?<><p className="eyebrow">MY CUP PROFILE · THIS DEVICE</p><h1>สวัสดี {name} ☺</h1><p>แก้วและหนังสือที่คุณเก็บจะอยู่ในอุปกรณ์นี้ก่อน โดยไม่ต้องสร้างบัญชี</p><div className="profile-stats"><div><b>0</b><span>แก้วที่เก็บไว้</span></div><div><b>10</b><span>คำตอบล่าสุด</span></div><div><b>0</b><span>Cup Reviews</span></div></div><h2>แก้วของฉัน</h2><p className="empty-profile">ยังไม่มีแก้วที่เก็บไว้ — กลับไปชงแก้วแรกของคุณกัน</p><div className="profile-actions"><a href="/#brew">ชงแก้วใหม่</a><button onClick={()=>{localStorage.removeItem("cup-of-us-name");setName("")}} type="button">ล้างโปรไฟล์บนอุปกรณ์นี้</button></div></>:<><p className="eyebrow">KEEP YOUR CUPS</p><h1>เรียกคุณว่าอะไรดี?</h1><p>ตั้งชื่อสั้น ๆ เพื่อเริ่มเก็บแก้วบนอุปกรณ์นี้ ไม่ต้องใช้อีเมลหรือรหัสผ่าน</p><div className="local-signup"><input value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>e.key==="Enter"&&save()} placeholder="ชื่อหรือ nickname" aria-label="ชื่อโปรไฟล์"/><button onClick={save} type="button">เริ่มเก็บแก้ว →</button></div><small>ข้อมูลนี้ยังไม่ซิงก์ข้ามอุปกรณ์ ระบบบัญชีจะเพิ่มภายหลังเมื่อพัฒนาเป็นแอป</small></>}</section></main>}
