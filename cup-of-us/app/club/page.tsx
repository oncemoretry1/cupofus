"use client";
import { useEffect,useState } from "react";
import "./club.css";
type Post={id:number;kind:string;title:string;body:string};
export default function ClubPage(){const [posts,setPosts]=useState<Post[]>([]);const [title,setTitle]=useState("");const [body,setBody]=useState("");const [kind,setKind]=useState("book");const load=()=>fetch("/api/community").then(r=>r.json()).then(d=>setPosts(d.posts??[]));useEffect(()=>{load()},[]);const publish=async()=>{let guestId=localStorage.getItem("cup-of-us-guest");if(!guestId){guestId=`guest-${crypto.randomUUID()}`;localStorage.setItem("cup-of-us-guest",guestId)}const response=await fetch("/api/community",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({guestId,kind,title,body})});if(response.ok){setTitle("");setBody("");load()}};return <main className="club-page"><nav className="catalog-nav"><a className="logo" href="/"><span>CUP</span><i>of</i><span>US</span></a><div><a href="/discover">Discover</a><a href="/brew">Brew yours</a><a className="profile-icon" href="/profile" aria-label="โปรไฟล์">●</a></div></nav><header><p className="eyebrow">CUP (CLUB) OF US</p><h1>เรื่องดี ๆ<br/><em>ยิ่งจิบยิ่งอยากแชร์</em></h1><p>ป้ายยาหนังสือ เพลง หนัง พอดแคสต์ หรือคาเฟ่ที่ช่วยให้วันหนึ่งของคุณดีขึ้น</p></header><section className="club-compose"><div><p className="eyebrow">BREW A POST</p><h2>วันนี้อยากป้ายยาอะไร?</h2></div><form onSubmit={e=>{e.preventDefault();publish()}}><select value={kind} onChange={e=>setKind(e.target.value)}><option value="book">หนังสือ</option><option value="podcast">Podcast</option><option value="music">เพลง</option><option value="movie">หนัง</option><option value="cafe">คาเฟ่</option></select><input value={title} onChange={e=>setTitle(e.target.value)} required placeholder="ชื่อสิ่งที่อยากแชร์"/><textarea value={body} onChange={e=>setBody(e.target.value)} required placeholder="มันช่วยคุณในวันแบบไหน?"/><button type="submit">PUBLISH CUP →</button></form></section><section className="club-feed"><div className="club-feed-head"><p className="eyebrow">FRESHLY BREWED BY US</p><h2>แก้วล่าสุดจากชุมชน</h2></div><div>{posts.length?posts.map(post=><article key={post.id}><span>{post.kind.toUpperCase()}</span><h3>{post.title}</h3><p>{post.body}</p><footer>จากสมาชิก Cup of Us · ♡</footer></article>):<p className="club-empty">ยังไม่มีโพสต์ใหม่ ลองชงโพสต์แรกของคลับได้เลย</p>}</div></section></main>}
